# 0.8.0：照片优先的界面与视频样式

## 本次更新

- 故事首页以大封面、渐变文字层和简洁信息栏展示回忆，搜索按需展开；保留故事、草稿和原有编辑入口。
- 图文时间线按图片比例显示，不裁掉横竖照片；同一天的连续片段不重复显示日期。
- 相册直接切换全部、照片、视频；保留高级类型筛选和网格、列表功能。
- 视频相册新增纸质相册、光影故事、简洁纪实三种样式。样式只影响分享视频，不修改故事数据。
- 分享页直接显示成品封面，点击即可预览；主操作固定在底部，保存与其他分享并列。
- 保留原声开关、完整视频、720×1280 输出、生成进度和取消操作。背景音乐仍已移除。

## 使用

打开故事 → 生成视频，分享给亲友 → 选择样式 → 生成分享视频 → 点画面预览 → 分享到微信。

已有视频时可点“更换相册样式”重新生成。不同样式分别记录缓存，旧成品不会被覆盖。
缓存可能被系统清理；长期保留请保存到手机。视频仅用于观看，恢复故事仍需要 ZIP 备份。

## 验证

- 13 项 JVM 故事测试通过；Debug、AndroidTest 和签名 Release 构建成功。
- 安卓设备导出纸质纯照片、纸质混合原声、纸质静音、光影混合、纪实混合五份 MP4；检查编码、尺寸、时长、音轨及非空白帧，全部完整解码通过。
- 相册三种快捷分类已复测，实际筛选结果与选择状态一致；修复上游重建列表布局时覆盖分类栏的问题。
- 正式包覆盖安装成功，版本 0.8.0 / 12，APK v2 签名校验通过。
- 检查横竖图文排版、真实成品预览、系统分享面板、1.3 倍系统字体和长标题。
- 用 10 张照片、每张 60 秒的临时故事检查生成取消；取消后返回选择页，不出现迟到的完成回调。
- 测试故事检查结束后按字节恢复原索引。预览图使用既有静物素材和人工测试画面，不含私人照片。
- 模拟器未安装微信，未验证微信内实际发送、接收和压缩。生成仍须留在页面，本次未新增后台导出或云同步。

## GitHub 发布

发布签名 APK 与对应源码；使用 GitHub Releases 托管，不依赖本机预览服务。
正式包保持原签名，可覆盖升级。不要卸载旧版本再安装。
